# COMPASS

<!-- scaffold:section=purpose -->

One-line purpose: <what this project is, in a single sentence>.

Read this file first. It points; it does not explain.
Once you are in the right directory, work the rest out from the code.

Hard cap: 100 lines. If something does not fit, it belongs in the file this points at.

<!-- scaffold:section=map -->

## Map

| Path         | What it is | Open it when            |
| ------------ | ---------- | ----------------------- |
| `<path>`     | <one line> | <the trigger to go here> |
| `<path>`     | <one line> | <the trigger to go here> |

A row pointing at a path that no longer exists is a defect.
Fix it in the same change that moved the file.

<!-- scaffold:section=running -->

## Running and testing

- Run: `<command>`
- Test: `<command>` (tests live in `<path>`)
- Lint: `<command>`

<!-- scaffold:section=siblings -->

## The other files

| Path               | Read it when                                           |
| ------------------ | ------------------------------------------------------ |
| `CLAUDE.md`        | Always, before doing anything                          |
| `issues/`          | Something is behaving oddly - newest 10 entries only   |
| `backlog/`         | Deciding what to work on - `done/` newest 10 only      |
| `NAMING.md`        | Before naming any file, symbol, branch, or ID          |
| `CONTEXT_STATE.md` | Starting a session - top 10 checkpoints only           |
