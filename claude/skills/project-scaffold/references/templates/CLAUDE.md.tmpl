# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this repository is

TODO: FILL THIS IN

## Hard rules

This holds even when something is plainly wrong.
If you spot something, say nothing.

"Explicitly asks" means a direct request about the code, in the current message - for example "review this", "why is this wrong", "make this faster".
A vague or adjacent instruction is not permission.
Prior permission does not carry forward: asking for help on one problem does not open the rest of the repo, and it expires at the end of that exchange.

When help is requested, answer only the question asked and stop.

## Hard rule: sliding-window files are read 10 entries deep, no further

Four files in this repository are sliding windows. Newest entry is always on top.

| File               | Retains               | Read                  | Holds                                          |
| ------------------ | --------------------- | --------------------- | ---------------------------------------------- |
| `ISSUES.md`        | 10 entries            | top 10                | What broke, why, and how it was fixed          |
| `CONTEXT_STATE.md` | 10 checkpoints        | top 10                | Session state at each checkpoint, newest first |
| `BACKLOG.md`       | 20 in `Done`          | top 10 of `Done`      | Work already shipped, newest first             |
| `HYDRATION.md`     | 10 entries            | **the top entry only**| The prompt that starts the next session        |

`HYDRATION.md` is the exception worth reading twice: it retains ten and is read **one** deep. Each
entry is a complete session-starting prompt, so ten of them is roughly fifteen thousand words of
superseded instructions. The nine below the top are kept so a question about how we got here can be
answered, not so they can be read at the start of a session.

On first read of any of them, take the **top 10 entries and stop**. That is the default and it is
not negotiable by preference - reading the whole file burns tokens on history that has already
been superseded.

`BACKLOG.md` is the partial case: `Now`, `Next` and `Later` are live work, so read them in full -
they are capped by the rules below, not by a window. The window applies to `Done` only, which is
the part that grows without bound.

This applies from the first checkpoint onward. `CONTEXT_STATE.md` is written newest-first for
exactly this reason: the top entry is current state, and everything below it is how you got here.

Go deeper only when one of these is true:

- The user asks you to.
- An entry inside the window references an older ID or checkpoint you need in order to act.
- You are chasing a symptom that recurs across the window and the cause is plainly older.

State it when you go deeper, and say why. Silently scanning the whole file is a defect.

Nothing in either file is ever rewritten in place. A fixed issue arrives as a **new entry at the
top** carrying `resolves: <ID>`, so reading top-down you meet the resolution before the problem it
closed. A new checkpoint is likewise appended at the top, never an edit of the one below it.

## What agents are for here

Menial, mechanical, non-coding work:

- Git: branches, commits, pushes, PRs, post-merge cleanup
- Documentation: README, this file
- Configuration: `.gitignore`, `.claude/`, tooling config
- Filesystem chores: creating problem directories, moving or renaming files as instructed

## Hard rule: use Lavish for anything worth looking at

Plans, comparisons, option sets, reviews, reports, and any question with more than one
axis go through Lavish - not a wall of terminal text.
It is smoother to read and lets me annotate the exact line I care about.

- Build the artifact under `.lavish/` in this project, then run `lavish-axi <html-file>`.
- Run `lavish-axi poll <html-file>` afterwards and leave it running until feedback arrives.
- Run `lavish-axi playbook <id>` first - read every playbook that applies to the artifact,
  not just one. Use the `input` playbook whenever the artifact asks me to decide something.
- Prefer Lavish over `AskUserQuestion` when the decision has options, tradeoffs, or
  formats I need to see side by side.

This is a hard rule, not a preference. Falling back to plain text for a multi-part
decision is a defect.

## Hard rule: no PII

Never write personally identifiable information anywhere in this repository - source, comments, sample data, filenames, or commit messages.
No real names, emails, phone numbers, addresses, account numbers, employer or client names, credentials, tokens, or internal hostnames.
All sample data must be synthetic and obviously fake.

If a request would introduce PII, substitute a placeholder and say so.

## Layout and running

## Working style here

Do the menial task you were given, confirm it is done, and stop.

## Zenith agent instructions

These are common instructions for Zenith's agents across all scenarios.

General Guidelines

- Never use the em dash "—". Use plain dash "-" instead
- When writing commit messages, NEVER auto-add your agent name as co-author
- Never manually modify CHANGELOG.md files or any files that are marked as auto-generated
- When writing or substantially editing long Markdown files, put each full sentence on its own line.
  Preserve normal Markdown structure, but avoid wrapping multiple sentences onto one physical line.
- When making technical decisions, do not give much weight to development cost.
  Instead, prefer quality, simplicity, robustness, scalability, and long term maintainability.
- When doing bug fixes, always start with reproducing the bug in an E2E setting as closely aligned with how an end use
  This makes sure you find the real problem so your fix will actually solve it.
- When end-to-end testing a product, be picky about the UI you see and be obsessed with pixel perfection.
  If something clearly looks off, even if it is not directly related to what you are doing, try to get it fixed along
- Apply that same high standard to engineering excellence: lint, test failures, and test flakiness.
  If you see one, even if it is not caused by what you are working on right now, still get it fixed.

Zenith Opinions

When you are working on something that would benefit from being informed by Zenith's viewpoints, read ./OPINIONS.md to understand.
If I have a new opinion and the file doesn't exist, we need to create and ask the user if they want this added to OPINIONS.md

## Post-merge cleanup

When the user says a PR is merged, perform this cleanup automatically without being asked.
Do not wait for the user to spell out the steps each time.

1. `git fetch origin --prune`
2. `git checkout main`
3. `git merge --ff-only origin/main` to fast-forward local `main` to the merged state.
4. Delete the feature branch locally: `git branch -D <feature-branch>`.
   Use `-D`, not `-d`: squash and rebase merges rewrite the SHA, so `-d` refuses the delete even though the work landed.
5. Delete the feature branch on the remote: `git push origin --delete <feature-branch>`.
6. Verify the result: on `main`, in sync with `origin/main`, and the feature branch gone from both local and remote.
7. Remove any temporary or scratch directories and scaffolding created during the work.

## Session State

See `CONTEXT_STATE.md` for current state, active tasks, decisions, and lessons learned.
Read it before starting any task - top 10 checkpoints only, per the sliding-window rule above.

It is written by the `context-compaction` skill at the end of a piece of work, immediately before
the PR is handed over, so the state that reaches `main` describes the change that reached `main`.

## Logging an issue

Never hand-edit `ISSUES.md` or `BACKLOG.md`. Both have scripts that own their format:

```sh
.claude/scripts/log-issue.sh --project . --help
.claude/scripts/backlog.sh   --help
```

`log-issue.sh` allocates the ID, stamps the time, and inserts at the top.
A fix for an earlier issue is a new entry with `--resolves ISS-NNNN`, never an edit of the old one.

Both scripts take `--json` and use documented exit codes: 0 ok, 2 usage, 3 validation, 4 io,
5 lock timeout, 6 id not found. Branch on the code, not on the message text.

## The close-out flow

**Everything the ticket owns happens on the work branch, and the ticket reaches `done` there.**
Then one pull request, then the merge, then the handback.

We always roll forward.
Nothing about a ticket is left as paperwork that follows the PR, because paperwork that follows a merge is paperwork that gets skipped.

```text
close-out
│
├─ ON THE WORK BRANCH ─── everything the ticket owns, nothing deferred
│  │
│  ├─ 1. CONTEXT_STATE.md        new checkpoint at the TOP, newest first
│  │
│  ├─ 2. THE TICKET REACHES done ← every part of it, in this order
│  │     ├─ evidence every AC      work-order.sh evidence --observed
│  │     ├─ interview-ready retro  work-order.sh note
│  │     └─ mark it done           work-order.sh done
│  │
│  └─ 3. HYDRATION.md            hydration.sh check, then add
│
├─ ONE PULL REQUEST
│  └─ 4. push once               code + ticket + state + prompt, one review
│
└─ AFTER THE MERGE
   ├─ 5. archive                 work-order.sh close, straight to main
   ├─ 6. cleanup                 ff main, delete the branch both sides
   └─ 7. hand back               the prompt AND its launch command, then hold
```

**There is exactly one pull request per ticket.**
A second PR carrying only state files doubles the review surface for one piece of work and leaves `main` briefly describing a world that no longer exists.

1. Update `CONTEXT_STATE.md` **on the work branch, as part of the work**.
   New checkpoint at the **top**, newest first, per the sliding-window rule above.
   Never append to the bottom and never edit the checkpoint below it.

2. **The ticket is marked `done` on the same branch, and everything it needs to get there is done first.**
   Evidence every acceptance criterion with `--observed`, including what the command or console actually printed.
   If a criterion is not met, say so plainly rather than dressing it up - an unmet criterion recorded honestly is worth more than a met one that is not true.
   Leave the interview-ready retro as a note: what went well, what went wrong, what was harder than expected, and STAR-shaped talking points with real specifics.
   Then `work-order.sh done`.

   ```sh
   WO=.claude/skills/work-order/scripts/work-order.sh
   bash $WO evidence --id WO-... --index N --observed "..."
   bash $WO note     --id WO-... --text "RETRO ..."
   bash $WO done     --id WO-...
   ```

   This is the rule the rest of the flow depends on.
   A branch that carries finished code and an unfinished ticket is a branch that merges into a `main` where the work exists and the record of it does not.

3. Generate the successor's hydration prompt **on the same branch**:

   ```sh
   HP=.claude/skills/hydration-prompt/scripts/hydration.sh
   bash $HP check --project . --body-file /tmp/entry.md    # refuses a malformed entry
   bash $HP add   --project . --id WO-... --title "..." --body-file /tmp/entry.md
   ```

   Never hand-edit `HYDRATION.md`. The script owns the ordering and the 10-entry window.

4. Push once and open **one** pull request.
   It carries the code, the finished ticket, the state file and the hydration prompt together, so one review sees the change and the whole record of the change.

5. After the merge, run `bash .claude/skills/work-order/scripts/work-order.sh close --id WO-...`.
   It backfills the merge SHA, archives the ticket and regenerates `INDEX.md` **straight to `main`** - no second PR.
   It falls back to a PR only if that push is rejected.

6. Post-merge cleanup: fast-forward `main`, delete the feature branch locally and on the remote.

7. Hand back **both** the hydration prompt and the command that starts the next session, then hold.
   Do not start it.

   ```sh
   bash $HP command --project .
   ```

**Never open a PR whose only content is `CONTEXT_STATE.md` or `HYDRATION.md`.**

**Check whether your PR has already merged before every commit once it is open.**
A squash merge rewrites the SHA and closes the branch, so a commit pushed seconds later is stranded
on a branch nothing points at. This has happened twice. Recovery is cheap - fast-forward `main`,
branch fresh, cherry-pick, delete the merged branch on both sides - but noticing late is avoidable,
and it is the usual reason a step gets skipped and the one-PR rule gets broken.

## Session start - skill version check

Skills are installed into a project as **copies**.
A copy has no way of knowing the original moved on, so this check is the only thing standing between a project and a silently stale skill.
Run it once, before the first task of the session.

Skip the whole thing if `.claude/skills/` does not exist.

**1. Read both sides.**

```bash
curl -fsS --max-time 5 https://raw.githubusercontent.com/jkkelley/dotfiles/main/claude/skills/registry.json
grep -H '^version:' .claude/skills/*/SKILL.md
```

That URL is literal and correct.
It is **not** a placeholder to be substituted with your own handle.
Substituting it points the check at a repository that does not exist and breaks it silently.

**2. Compare.**

- A skill with no `version:` line predates versioning. Treat it as behind.
- A skill installed in the project but absent from the registry is project-local. Ignore it silently.
- If the fetch fails, say one line - `skill version check skipped, registry unreachable` - and start the work. Never block a session on this.

**3. If everything matches, say nothing.**
Start the work.
No output, no confirmation, no "all skills current" line.

**4. If anything is behind, print the gap, then ask.**
One table, then exactly three options, then wait for an answer.

| skill            | installed | latest | bump  |
| ---------------- | --------- | ------ | ----- |
| hydration-prompt | 1.1.0     | 1.4.0  | minor |

1. **Update now.** Standalone, before the work starts.

   ```bash
   curl -fsSL https://raw.githubusercontent.com/jkkelley/dotfiles/refs/heads/main/claude/skills/skill-versioning/scripts/skill-update.sh -o /tmp/skill-update.sh
   bash /tmp/skill-update.sh --skill <name> --mode standalone --project .
   ```

   Branches off `origin/main` inside a throwaway worktree, copies, commits, pushes, opens a PR, squash-merges it without review, deletes the branch, fast-forwards local `main`, and prints the PR URL.
   The working tree is never touched, so work in progress is safe.
   Merged without review on purpose: the content is a byte copy of a file already reviewed in dotfiles, so there is nothing left for a human to decide.

2. **Fold it into this work.** The update rides the commit that is about to happen anyway.

   ```bash
   curl -fsSL https://raw.githubusercontent.com/jkkelley/dotfiles/refs/heads/main/claude/skills/skill-versioning/scripts/skill-update.sh -o /tmp/skill-update.sh
   bash /tmp/skill-update.sh --skill <name> --mode inline --project .
   ```

   Copies the skill in and leaves it uncommitted.
   No branch, no PR, no round trip.
   Then get straight on with the actual task.

3. **Note it and move on.**
   The table above was the whole deliverable.
   Write nothing, start the work.

Report the outcome plainly: the new version and the full path on success, or the failing step and the log path on failure.
The script captures both.

All three options work on any machine with `curl` and `git`.
The script is fetched from GitHub and it fetches the skill from GitHub, so nothing here needs a dotfiles checkout.
If this project already vendors `skill-versioning`, run `.claude/skills/skill-versioning/scripts/skill-update.sh` instead and skip the download.

## Starting a session

`HYDRATION.md` is the prompt that starts the next session, and the nine before it.

**`HYDRATION.md` is the instruction. `CONTEXT_STATE.md` is the background it assumes.**
Both are written in the same commit, by the same close-out, describing the same moment, and an agent reads both.
**When they disagree, the hydration prompt wins.**

That is not a tie to be reconciled and not a judgement call.
`CONTEXT_STATE.md` is a state snapshot - where things stood, what was decided, what was learned.
`HYDRATION.md` is an instruction written for this session - what to do, what not to assume, what must be settled first.
State describes; the prompt directs.
A snapshot that has drifted is stale information, which is survivable.
An instruction overridden by stale information is how a session goes and does the wrong thing confidently.

Read the hydration prompt first, then the state file for context.
If the two contradict, follow the prompt and **say so** - two files written in the same commit that disagree is a defect worth reporting, not worth silently resolving.


**Read the top entry only.** It is current and complete on its own; everything below it has been
superseded and is kept for history, not for reading. This differs from `CONTEXT_STATE.md` and
`ISSUES.md`, which are read ten deep - ten full hydration prompts is roughly fifteen thousand words
of superseded instructions.

Newest on top. Adding an entry removes the tenth in the same commit. Entries are never numbered,
never renumbered and never edited in place; a correction is a new entry. The `hydration-prompt`
skill owns the file.
