---
{
  "id": "WO-20260824-b21b",
  "slug": "claude-md-tmpl-replace-the-session-start-prose-a",
  "title": "CLAUDE.md.tmpl: replace the session-start prose, add the skills markers, the treehouse policy and the documentation-lifetime rule",
  "type": "chore",
  "status": "in-progress",
  "priority": "p2",
  "created": "2026-08-24",
  "updated": "2026-08-30",
  "created_at": "2026-08-24T13:19:12-05:00",
  "parent": "WO-20260824-00d5",
  "branch": "feat/claude-md-tmpl-replace-the-session-start-prose-a",
  "pr": null,
  "merge_sha": null,
  "closed": null,
  "approval": {
    "via": "override",
    "reason": "Reviewed and approved on PR #55 on GitHub, which is where the whole cut was read as one diff. Lavish was offered and declined in favour of the PR.",
    "at": "2026-08-24"
  },
  "evidence": null,
  "surfaces": [],
  "depends_on": [
    "WO-20260824-9712",
    "WO-20260824-81a6"
  ],
  "blocks": [
    "WO-20260824-238b"
  ]
}
---

# WO-20260824-b21b - CLAUDE.md.tmpl: replace the session-start prose, add the skills markers, the treehouse policy and the documentation-lifetime rule

## Problem

The template carries roughly 68 lines of session-start prose that this design deletes outright. It also has nowhere for the generated skills table to land, says nothing about where a workspace comes from, and leaves the conflict between local-k8s-docs and living-docs over who writes into docs/sops/ for each project to rediscover.

## Scope

**In**

- replacing CLAUDE.md.tmpl lines 269 to 336 with the short block from the design doc
- skills:begin and skills:end markers, written by the sync and not by scaffold.sh
- a treehouse policy section naming the user-level pool per decision 19 and pointing at treehouse status
- the documentation-lifetime rule that arbitrates docs/sops/

**Out - non-goals**

- versions in the generated table - names only, because a hand-maintained version table is wrong within a week

## Acceptance criteria


- [x] `AC-H1` *(human)* the template names exactly one destination for a document and one source for a workspace
  - observed `2026-08-30` observed 2026-08-30, project-scaffold suite via bump-gate.sh run-suite, 187 + 9 checks, 0 failed, "all cases passed, both images". One destination for a document: case 150-documentation-lifetime, 12 checks, scaffolds a project and reads its CLAUDE.md. The heading "## Hard rule: documentation goes where its lifetime says it goes" is present, the deciding question "would this still be true after this repo is deleted?" is present, and "every document has exactly one destination" is asserted as a string rather than inferred from the table. Both destinations resolve - the literal https://github.com/jkkelley/local-k8s-docs with its real owner, not an angle-bracket placeholder, and `docs.sh sop` named for the in-repo half - so the question has somewhere to route to in either direction. The negative closes it: assert_not_contains on "## Hard rule: every runbook and playbook lives in one repository", the unarbitrated rule this replaced, so the conflict cannot come back green. One source for a workspace: case 170-treehouse-policy, 7 checks, new in this ticket. "## Where your workspace comes from" is present, "One source: the treehouse pool at" and the decision-19 path ~/.treehouse/<repo>-<hash>/ are both asserted, and the two alternatives are refused by name - "Never hand-roll a `git worktree add`" and "never create a second pool inside the repository". The last check asserts destroy --force is absent, which is how the section is held to being a pointer at `treehouse status` rather than a copy of an interface that went v1.8.0 to v2.3.0 in a morning.
- [x] `AC-H2` *(human)* the generated table sits between markers that the sync writes, and scaffold.sh writes no table
  - observed `2026-08-30` observed 2026-08-30, case 140-skills-block, 9 checks, 0 failed, inside the project-scaffold suite run above. Scaffolds a project and reads its CLAUDE.md. Both markers are asserted byte for byte - "&lt;!-- skills:begin - generated by skill-sync, do not hand-edit --&gt;" and "&lt;!-- skills:end --&gt;" - as literal needles rather than fragments, because skill-sync matches on that comment text and a reworded marker is the sync silently losing its anchor rather than a cosmetic edit. The opening marker names skill-sync in its own text, so the template states the ownership rather than leaving it to the plan. "scaffold.sh writes no table" is asserted on the data, not by absence of a heading: awk extracts everything strictly between the two markers, tr strips whitespace, and assert_eq requires the result to be the empty string, so any row, pipe or stray word appearing there fails. Two negatives guard the direction this could regress in - assert_not_contains on the old "| skill | installed | latest | bump |" header, and on the registry.json URL the deleted 68-line check had the agent fetch by hand. Read honestly, one clause of this criterion is a contract and not yet a behaviour: nothing writes between the markers today. Verified 2026-08-30 across every open and archived ticket that no other work covers it - WO-20260824-5b89 and WO-20260824-efb0 both name writing outside .claude/skills/ as an explicit non-goal - so WO-20260830-eb89 - skill-sync fills the CLAUDE.md skills table between the markers was cut on this branch, parented to the epic, ordered after WO-20260824-238b, and recorded as a note on the epic. What this ticket delivers and what the case proves is the ownership: the markers exist, they name skill-sync, and scaffold.sh writes nothing between them.

## Test plan

_none recorded - Rule 14 says this runs in a container_

## Assumptions

_none_

## Open questions

_none_

## Notes

_Newest first. Appended only by `work-order note` - never by hand._

- `2026-08-30` RETRO. What went well: the four edits were genuinely independent, so each landed as its own Edit against an exact string and the deleted-line audit (git diff -U0 | grep ^-) accounted for every removed line in one pass, with no formatter damage on either file - the trap the last two cycles paid for did not fire here. Reading skill-sync.sh render_notice before writing markers for it, as the hydration prompt instructed, is what surfaced the real finding rather than shipping a marker pair and assuming somebody filled it. What went wrong: I did not check the test suite before editing the template, so the first suite run would have failed on two cases asserting sections I had just deleted. Grepping the suite for the heading text I was about to remove takes ten seconds and would have made cases 140 and 150 part of the plan rather than a discovery. Cheap here because nothing was committed; expensive on a ticket where the suite run is the last step before a push. Harder than expected: the ticket was sized as 5 points of prose and is not - two cases had to be rewritten end to end, a third written from scratch, and three SOP sections rewritten with their why paragraphs, because in this repo a template assertion is the template's contract and deleting a section without its case leaves the replacement unguarded. Actual shape was prose plus roughly 200 lines of test and test documentation. STAR. Situation: E2.4 said add skills:begin and skills:end markers and named skill-sync as the writer; AC-H2 said the generated table sits between markers that the sync writes. Task: ship the markers and evidence the criterion honestly. Action: grepped every open and archived work-order plus the plan and the design doc for who writes the table, found the two skill-sync build tickets both list writing outside .claude/skills/ as an explicit non-goal, and put the fork to the user rather than either widening the ticket unasked or ticking a criterion that was half contract and half unbuilt behaviour. Result: markers shipped with case 140 asserting the block between them is empty on the data, WO-20260830-eb89 cut on this branch for the fill, the epic noted, and AC-H2 evidenced with the unbuilt clause named in the evidence rather than papered over. Second STAR. Situation: local-k8s-docs and living-docs both claimed docs/sops/ and neither deferred. Task: arbitrate, not describe. Action: took the design doc's lifetime question as the discriminator, wrote 'every document has exactly one destination' as an assertable sentence, and kept the three rules the old section carried that the design block had dropped - runbooks and playbooks named together, ask rather than route around, and do not invent a local procedure when a documented one exists - since losing a real rule is a regression rather than a simplification. Result: case 150 asserts the arbitration positively and asserts the old unarbitrated heading is gone, so the conflict cannot return green.
- `2026-08-30` The markers ship inert and that is deliberate. skills:begin and skills:end land in CLAUDE.md.tmpl with skill-sync named as their writer, and nothing writes between them yet. AC-H2 reads 'the generated table sits between markers that the sync writes, and scaffold.sh writes no table', which on a literal reading needs skill-sync changed in this branch. The user was asked on 2026-08-30 and chose markers only, plus a ticket: WO-20260830-eb89 - skill-sync fills the CLAUDE.md skills table between the markers, cut on this branch, parented to the epic and ordered after WO-20260824-238b - Rename skill-versioning to skill-registry, the closing commit. So this ticket evidences AC-H2 as the ownership contract - the markers exist, they name skill-sync, scaffold.sh writes nothing between them and case 140 asserts that block is empty - and the fill itself is eb89.
- `2026-08-24` Poker 2026-08-24: 5 points. Sized above the plan's small and kept out of the bundle. Prose, four distinct edits, and it arbitrates a real conflict over docs/sops/.

## Outcome

_Written by `work-order close`. Empty until then._
